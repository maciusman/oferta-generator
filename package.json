{
  "name": "oferta-generator",
  "version": "1.0.0",
  "description": "Generator interaktywnych ofert HTML z bookmarklet",
  "main": "api/generate.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "echo 'This is a serverless app'"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
